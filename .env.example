WORKER_URL=http://localhost:8080
HTTP_ADDR=:8082

# Kafka brokers/topic this service publishes download job requests to
# (the worker consumes them; the browser can't speak Kafka directly).
KAFKA_BROKERS=localhost:9092
KAFKA_JOBS_TOPIC=video.jobs
