WORKER_URL=http://localhost:8080
HTTP_ADDR=:8082

# RabbitMQ connection URL. POST /api/jobs publishes the job request to the
# "video.jobs" queue (the worker consumes it; the browser can't speak AMQP).
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
